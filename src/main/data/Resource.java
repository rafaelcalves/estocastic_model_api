package main.data;

public class Resource {

    /*Propriedades
    o name: string
    o id: integer  atribuído pelo Scheduler
    o quantity: integer  quantidade de recursos disponíveis
    • Métodos
    o Resource(name, quantity)
    o allocate(quantity): boolean  true se conseguiu alocar os recursos
    o release(quantity)
    coleta de estatísticas
    o allocationRate(): double  percentual do tempo (em relação ao tempo total simulado) em que estes
    recursos foram alocados2
    o averageAllocation(): double  quantidade média destes recursos que foram alocados (em relação ao
    tempo total simulado)*/
}
